阶段 04 ESLint 反向验证（临时错误，已立即还原）

命令：npm exec --yes --package=pnpm@11.21.0 -- pnpm --filter @financehot/web lint
临时错误：在 apps/web/components/route-error.tsx 临时加入 `<a href="/">`。
退出码：1
关键输出：
  32:11  error  Do not use an `<a>` element to navigate to `/`. Use `<Link />` from `next/link` instead.  @next/next/no-html-link-for-pages
  ✖ 1 problem (1 error, 0 warnings)
  [ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL] ... lint: `eslint .`
  Exit status 1
